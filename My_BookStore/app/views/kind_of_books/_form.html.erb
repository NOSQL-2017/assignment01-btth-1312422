<%= form_for(kind_of_book) do |f| %>
  <% if kind_of_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kind_of_book.errors.count, "error") %> prohibited this kind_of_book from being saved:</h2>

      <ul>
      <% kind_of_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
